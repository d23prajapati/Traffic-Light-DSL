[comment encoding = UTF-8 /]
[module generateEnumStates('http://www.example.org/trafficLightSystem')]

[import org::eclipse::acceleo::module::trafficLightSystem::requests::getInitialState /]

[template public generateEnumStates(aStateMachine : StateMachine)]
enum TrafficState {
	[for (aState : State | aStateMachine.states) separator (',\n')]
		STATE_[aState.name.toUpper()/]
	[/for]
};

[/template]
